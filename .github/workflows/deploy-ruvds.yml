name: Retired Aida production deploy

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    name: Production deploy retired
    runs-on: ubuntu-latest
    steps:
      - name: Explain source of truth
        run: |
          echo 'Production deployment from Aida2-0- is retired.'
          echo 'Svetlanandreeva/Aida-Hels main is the only production source of truth.'
